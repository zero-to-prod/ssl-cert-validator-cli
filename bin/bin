#!/usr/bin/env php
<?php

use Symfony\Component\Console\Application;
use Zerotoprod\:package_namespace\:package_namespace;

require getcwd().'/vendor/autoload.php';

$application = (new Application());

$application->add(new :package_namespace());

$application->run();